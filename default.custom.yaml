patch:
  __include: rime_ice_suggestion:/
  schema_list:
    - {schema: tiger}
    - {schema: tigress}
    - {schema: PY_c}
  "switcher/hotkeys":  # 将默认的 F4热键 更改为ctrl+~
    - "Control+grave"
    - "Control+F8"
    # - "F4" 
    # 如果想用F4来切换，把上一行，行首的#删去
